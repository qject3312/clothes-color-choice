[일반 실행]
1. install_requirements.bat 실행
2. start_app.bat 실행
   - 백엔드는 앱이 자동으로 켭니다. start_backend.bat를 따로 켤 필요가 없습니다.

[실행파일 만들기]
1. build_exe.bat 실행
2. dist 폴더 안의 FitPickApp.exe 실행

[주의]
- 실행파일 빌드는 PC 환경에 따라 시간이 조금 걸릴 수 있습니다.
- exe 실행 시에도 clothes.db와 app_data/images가 앱 폴더 기준으로 생성/사용됩니다.
